#pragma once
#include <stdlib.h>

/**
 * @file Find String Length
 */

/**
 * @brief Finds the number of characters in a string
 *
 * @param stringStart points to beginning of a string
 *
 * @return size of given string 
 */
size_t strlen(const char* stringStart);

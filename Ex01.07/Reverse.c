#include <stdio.h>

int receiveInput()
{
    int usersNumber;
    scanf(" %d", &usersNumber);
    return usersNumber;
}

void continuousInput()
{
    int usersNumber = receiveInput();
    while (usersNumber != 0)
    {

    }
}